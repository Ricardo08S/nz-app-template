name: Deploy to VPS

on:
  push:
    branches:
      # development omitted on purpose: secrets/development/ does not exist
      # yet, so a push there would only trigger a job that fails validation.
      - production
    paths:
      - apps/server/**
      - apps/web/**
      - deploy/**
      - .github/workflows/**

permissions:
  contents: read

env:
  APP_ENV: ${{ github.ref_name }}

jobs:
  deploy:
    name: Deploy to VPS
    runs-on: self-hosted
    environment: ${{ github.ref_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # deploy.sh diffs migration files between this commit and the
          # previously-deployed one (state.json) to detect breaking
          # migrations (§4.7) — needs full history, not the default shallow
          # single-commit clone, or that diff would fail outright.
          fetch-depth: 0

      # server.sops.yaml references $POSTGRES_PASSWORD (from infra.sops.yaml)
      # and $APP_NAME/$BASE_DOMAIN (from deploy/env/production.env) — these
      # only resolve if all three land in the same .env, because
      # dotenv-expand (apps/server/src/env.ts) resolves $VAR against
      # whatever else it just loaded from that one file, not across files.
      - name: Decrypt and merge secrets
        env:
          SOPS_AGE_KEY: ${{ secrets.SOPS_AGE_KEY }}
        run: |
          set -euo pipefail
          cat deploy/env/${{ env.APP_ENV }}.env \
            <(sops -d --output-type dotenv secrets/${{ env.APP_ENV }}/infra.sops.yaml) \
            <(sops -d --output-type dotenv secrets/${{ env.APP_ENV }}/server.sops.yaml) \
            > apps/server/.env
          cat deploy/env/${{ env.APP_ENV }}.env \
            <(sops -d --output-type dotenv secrets/${{ env.APP_ENV }}/web.sops.yaml) \
            > apps/web/.env

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.18.3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate apps/server/.env against the runtime schema
        run: pnpm exec tsx scripts/validate-env.ts apps/server/.env

      # No GHCR: builds server/web images right here on the runner (which is
      # the VPS itself for a self-hosted setup) and hands them straight to
      # deploy.sh. See deploy/local-build-deploy.sh for why this needs no
      # changes to deploy.sh/docker-compose.app.yml — it only cares about
      # the image ref string, not where it came from.
      - name: Build and deploy (local images)
        run: bash deploy/local-build-deploy.sh "${{ env.APP_ENV }}"
